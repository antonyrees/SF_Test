Riverside Salesfore Technical Test

The following is a Salesforce Lightning Web Component (LWC) that we use in several pages, including the customer self registration process, called AddressFinder. It allows the user to enter a postcode and tries to match it against the properties Riverside manages. A list of potential matches is returned for the user to select with the selected property returned from the component. We’ve “accidently” broken the component and it’s controller class. Help us fix it. 

Also what if anything would you change/improve? 

